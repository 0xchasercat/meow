# Rust build artifacts
/target
**/target

# meow generated shadow configs / caches (ADR-8: gitignored build artifacts)
.meow/

# transient sub-agent worktrees (GIT.md)
.claude/worktrees/
# internal docs (not for public release)
harness/
COMPAT_LOG.md
/.claude/
scripts/

# node + os noise
node_modules/
.DS_Store
*.log
.commandcode/
