{
  "version": 1,
  "updated_at": "2026-06-08T03:00:00Z",
  "open": [
    {
      "id": "A-OBS1-PATHS",
      "kind": "assumption",
      "title": "why-dep defaults to ALL simple paths (not shortest)",
      "opened_at": "2026-06-07T00:00:00Z",
      "status": "open",
      "summary": "meow why-dep reports every simple root->target chain by default; --shortest opts into one representative.",
      "assumed": "Default PathMode::All because a package's full \"why\" is every owner that pulls it in (matches npm why / cargo tree -i); shortest hides co-owners. Exponential blow-up bounded by --limit (default 256) with an honest truncation note (I-11).",
      "shipped": "OBS-001",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "A-OBS1-ROOTS",
      "kind": "assumption",
      "title": "why-dep root-deps come from MeowConfig.dependencies, widening depends_on beyond [PKG-001]",
      "opened_at": "2026-06-07T00:00:00Z",
      "status": "open",
      "summary": "Assignment said depends_on [PKG-001]; the full CLI honestly needs [PKG-001, PKG-002, CFG-001] because the lockfile cannot mark direct deps.",
      "assumed": "Per PKG002 IRC, in-degree-zero is wrong (transitive-only pkgs can be in-degree-zero; direct deps can have in-edges). Correct root source = MeowConfig.dependencies pinned via Lockfile::resolve_roots (PKG-002) + MeowConfig::load (CFG-001). The pure path-finder library stays PKG-001-only.",
      "shipped": "OBS-001",
      "reversible": true,
      "confidence": "high"
    },
    {
      "id": "LSP-001-A1",
      "kind": "assumption",
      "title": "Shadow .meow/deps map is flat-by-name; multi-version collisions pick one + record the rest",
      "opened_at": "2026-06-07T00:00:00Z",
      "status": "open",
      "summary": "LSP-001's shadow node_modules symlink map exposes one version per package name (CANON \u00a720.1 flat-paths model); multi-version collisions are recorded, not nested.",
      "assumed": "Flat .meow/deps/<name> over the root-reachable closure matches CANON \u00a720.1's flat tsconfig-paths model; on a multi-version name, link the root-reachable (else lowest) version and record the rest in ShadowDeps::collisions. A nested per-package multi-version tree is a full-LSP refinement; the runtime resolver stays multi-version-correct via the lockfile regardless.",
      "shipped": "LSP-001",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "LSP-001-A2",
      "kind": "assumption",
      "title": "LSP holds its own Resolver built from the SHARED ResolutionGraph (parity by construction, not a shared Resolver instance)",
      "opened_at": "2026-06-07T00:00:00Z",
      "status": "open",
      "summary": "EditorResolver::resolve_module returns the runtime-identical url+locator plus an editor_path into the unpacked store; both sides build their own Resolver from one Arc<ResolutionGraph>.",
      "assumed": "The Resolver's RefCell PackageFs cache is single-thread (PKG-003), so the LSP and run-path share one Arc<ResolutionGraph> (identical inputs) rather than one Resolver instance. Parity is by construction and asserted by the corpus test. Surface = EditorResolver::resolve_module(specifier, &Url) -> EditorResolution { url, locator, editor_path }.",
      "shipped": "LSP-001",
      "reversible": true,
      "confidence": "high"
    },
    {
      "id": "LSP-001-A3",
      "kind": "assumption",
      "title": "Shadow symlink map is Unix-first; Windows (junctions vs --materialize) deferred",
      "opened_at": "2026-06-07T00:00:00Z",
      "status": "open",
      "summary": "The prototype ships Unix directory symlinks; Windows directory-symlink support is flagged for the human, not implemented.",
      "assumed": "Unix directory symlinks (std::os::unix::fs::symlink) ship in the prototype (dev target is darwin). Windows directory symlinks need Developer Mode/privilege; the planned path is directory junctions (no privilege) or falling back to `meow install --materialize`. Committing to junctions vs requiring --materialize on Windows is a product/platform call the full LSP must settle.",
      "shipped": "LSP-001",
      "reversible": true,
      "confidence": "low"
    },
    {
      "id": "PKG003-A1",
      "kind": "assumption",
      "title": "PnP feeds the Resolver by building+validating inputs (ResolutionGraph), not by modifying the resolver",
      "opened_at": "2026-06-07T00:00:00Z",
      "status": "open",
      "summary": "meow_pkg::ResolutionGraph (root_deps + Arc<Lockfile> + validated reachable closure) is fed unchanged through the single seam Resolver::from_resolution(&graph, cache, project_root, native); the LOAD-003 algorithm is untouched and node_modules is never read.",
      "assumed": "The PnP<->Resolver integration fork (ADR-4): PnP is a data producer/validator, not a second resolution path. The resolver already keys first-party off root_deps and cached-referrer deps off LockEntry.dependencies, so PKG-003 only supplies+validates those two inputs. Runtime and LSP build from the SAME Arc<ResolutionGraph> via from_resolution => identical inputs => identical resolution (I-5). node_modules is an export (PKG-004), never consulted (honest boundary, I-11). Coordinated over irc with PKG-004 (consumes ResolutionGraph via packages()), PKG-002 (produces lockfile+cache, owns pin_roots), LSP-001 (constructs only via from_resolution).",
      "shipped": "PKG-003",
      "reversible": true,
      "confidence": "high"
    },
    {
      "id": "PKG003-A2",
      "kind": "assumption",
      "title": "Root direct-dep pins are a pure function of (declared direct deps, lockfile) via resolve_roots; no separate persisted root index",
      "opened_at": "2026-06-07T00:00:00Z",
      "status": "open",
      "summary": "ResolutionGraph root_deps come from the SHARED meow_pkg::resolve_roots(direct, lockfile) = max-satisfying locked version per declared direct dep (owned by PKG-002, called by install/runtime/LSP/PnP); no extra on-disk artifact beyond meow.lock.jsonl + MeowConfig.dependencies.",
      "assumed": "The root has no LockEntry, so its exact direct-dep versions need a source. A pure function applied by every consumer cannot diverge (I-5/I-6 by construction). Honest caveat (I-11): max-satisfying lets a root dep silently float to a higher locked patch a transitive pulled in (deterministic, explainable via why-dep/OBS-001). Rejected heavier alternative: an install-written generated .meow/ PnP index (Yarn .pnp.cjs style). Shared root-derivation fn name reconciled to PKG-002's WRITTEN symbol meow_pkg::resolve_roots(direct: BTreeMap<PackageName,DepSpec>, lockfile) -> Result<_, RootResolveError> (supersedes the verbal pin_roots); OBS-001 already uses resolve_roots.",
      "shipped": "PKG-003",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "A-PKG002-1",
      "kind": "assumption",
      "title": "PKG-002 integrity: verify npm sha512 on download, content-address by meow sha256 (cache+lockfile key)",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "npm dist.integrity (sha512) is the supply-chain check at download; the meow sha256 is the cache key + LockEntry.integrity + I-7 anchor. sha512 verified-then-discarded at P2.",
      "assumed": "PKG-001 cache is sha256; npm SRI is sha512. Verifying npm then content-addressing by sha256 reuses the existing cache without extending HashAlgo; provenance (persist sha512) deferred to SEC-002.",
      "shipped": "PKG-002",
      "reversible": true,
      "confidence": "high"
    },
    {
      "id": "A-PKG002-2",
      "kind": "assumption",
      "title": "PKG-002/PKG-004 default install mode = PnP (ADR-4)",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "meow install defaults to PnP (in-memory, no node_modules); --materialize/--vendor are the escape hatch.",
      "assumed": "ADR-4 / CANON 24.4 ship PnP + materialize together with PnP as the norm.",
      "shipped": "PKG-002",
      "reversible": true,
      "confidence": "high"
    },
    {
      "id": "A-PKG002-3",
      "kind": "assumption",
      "title": "PKG-002 declared deps live in meow.config.dependencies; add/remove write meow.config.json pre-RT-001",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "meow.config is the single source of declared deps (new MeowConfig.dependencies map); install add/remove edits it (JSON pre-defineMeow-execution).",
      "assumed": "CFG is the single config authority; a TS-executed config is later, so JSON edits are the honest P1/P2 boundary.",
      "shipped": "PKG-002",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "A-PKG002-4",
      "kind": "assumption",
      "title": "PKG-002 HTTPS registry client = small blocking rustls client at the CLI edge only",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "The real NpmRegistry impl uses a small blocking HTTPS client (e.g. ureq) in the binary edge; meow-pkg stays network-free behind the RegistrySource trait.",
      "assumed": "Keeps meow-pkg footprint clean (I-10) + testable (FixtureRegistry); the client is swappable.",
      "shipped": "PKG-002",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "A-PKG002-5",
      "kind": "assumption",
      "title": "PKG-002 version resolution uses the pinned semver crate, not node-semver",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "Range resolution uses the workspace semver crate; node-semver-only forms error honestly (NoMatchingVersion/UnsupportedRange) rather than mis-resolve.",
      "assumed": "Avoids a node-semver reimplementation now; the honesty gate (I-11) covers the gap; a faithful engine is a later option.",
      "shipped": "PKG-002",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "A-PKG004-projection",
      "kind": "assumption",
      "title": "PKG-004 node_modules vs vendor = two flags, two projections of one ResolutionGraph",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "--materialize -> node_modules/ (tool-compat, symlink default); --vendor -> vendor/ (full copy, air-gap).",
      "assumed": "CANON 12.2 defines vendor as a full local copy; modelling both as projections of the shared graph keeps one resolver (I-5).",
      "shipped": "PKG-004",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "A-PKG004-layout",
      "kind": "assumption",
      "title": "PKG-004 isolated/pnpm-style no-hoist materialized layout",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": ".meow/<name>@<ver>/node_modules/<name> + per-package relative symlink edges; no hoisting.",
      "assumed": "Hoisting collapses versions so a foreign Node walker resolves a different version than meow PnP -> breaks resolver-parity (I-5). No-hoist preserves parity.",
      "shipped": "PKG-004",
      "reversible": true,
      "confidence": "high"
    },
    {
      "id": "A-PKG004-link",
      "kind": "assumption",
      "title": "PKG-004 link strategy: symlink default (materialize), copy default (vendor), --copy override",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "Edges are symlinks for --materialize, copies for --vendor; --copy forces copies; SymlinkUnsupported is typed (points at --copy), never a silent copy.",
      "assumed": "Cheapest faithful default per projection; honest typed error on symlink-hostile filesystems.",
      "shipped": "PKG-004",
      "reversible": true,
      "confidence": "medium"
    },
    {
      "id": "A-PKG004-vendor",
      "kind": "assumption",
      "title": "PKG-004 vendor = full self-contained copy (resolvable with the global cache absent)",
      "opened_at": "2026-06-08T03:00:00Z",
      "status": "open",
      "summary": "vendor/ is a complete copy that resolves air-gapped (global ~/.meow/cache removed), at the cost of disk duplication; an air-gap test is required.",
      "assumed": "Matches the vendor intent (portable/committable); duplication is the accepted trade.",
      "shipped": "PKG-004",
      "reversible": true,
      "confidence": "medium"
    }
  ],
  "resolved": []
}
