# Changelog

Auto-appended, one line per merge — `YYYY-MM-DD · <spec> merged · <what> · gates <verdict>`.
This is NOT a hand-curated narrative (that was the 258KB/139KB HANDOFF smell). The "where are we"
view is *derived* by Mission Control's Digest from `state.json`; this file is just the durable
changelog. Rotated to `changelog.archive/` past 200 entries.

<!-- 0xos:changelog:top -->
2026-06-09 · Symlink-first install pivot · Default `meow install` now writes the strict pnpm-style symlinked `node_modules` projection backed by the global unpacked store; deprecated `--mode pnp` is the explicit virtual mode and `--vendor` remains copy-based · gates: not run (commands prohibited).
2026-06-08 · TOOL-001/002/003 planning reconciled · P3 documentation now targets shared `crates/tool` (`meow-tool`) with OXC-recommended lint + OXC formatter slice and Rolldown bundle skeleton/CLI wiring, and explicitly defers PnP Resolver bridge finalization for a follow-up · gates: docs/plan update only.
2026-06-08 · Harness ledger settled for DR-003/PnP projection · Canon/Plan/state/decisions now keep ResolutionGraph runtime truth → UnpackedStore real paths → `.meow/deps` editor shadow → strict `node_modules` materialization; stale package.json-generation, synthetic-CJS-wrapper, and manual-polyfill targets marked superseded · gates: not run (ledger-only; commands prohibited).
2026-06-08 · PnP projection model baked in · ResolutionGraph is runtime truth; UnpackedStore backs Deno Node/N-API real paths; `.meow/deps` is symlink-first editor/TS/LSP projection with collision records; strict `node_modules` projection is the filesystem-first install surface · gates: docs/plan update only.
2026-06-08 · DR-003 accepted · Great Harvest direction recorded: Node compatibility delegates to upstream Deno crates (`deno_node`/`deno_napi`/crypto/fetch/net); meow owns PnP graph/cache/UnpackedStore bridge + UX; P2.5 resumes before P3 with RT-007/LOAD-004/RT-008 reoriented away from manual polyfills and synthetic CJS wrappers · gates: docs/plan update only.
2026-06-07 · planning complete — foundation set; autonomous execution begins. CONSTITUTION/PRODUCT/PLAN/PRINCIPLES/GATES/CRAFT Part B + CANON.md filled, traceable (I-1..I-11 ↔ 11 gates ↔ P12-P17); phase → P0 Foundations.
2026-06-07 · DIST-001 merged · Cargo workspace + meow CLI skeleton (18 honest stubs, EXIT_UNIMPLEMENTED=3; upstream V8 pins declared-not-consumed) · gates: floor green (build/fmt/clippy/test 4·4/principles), footprint green (gzip 392KB ≤ 60MB).
2026-06-07 · PKG-001 merged · meow-pkg: meow.lock.jsonl (strictly-sorted JSONL, semver-validated) + content-addressed cache with hash-integrity + self-heal (I-7) · gates: floor green, craft green (2 rounds, 3 findings fixed).
2026-06-07 · CFG-001 merged · meow-config: defineMeow serde model + shadow .meow/tsconfig.json gen + root shim (ADR-8) + wired `meow sync` · gates: floor green, craft green.
2026-06-07 · GRAPH-001 merged · meow-graph: Oxc parse pipeline (lossless CST / semantic / runtime-IR seam) as a hand-built incremental query DB — the single parser entrypoint (I-1); honest stage-5 rejects TS/errors until RT-003 (I-3/I-11) · gates: floor green (50 tests), craft green (2 blocks fixed: no-panic FileId, honest runtime_ir).
2026-06-07 · RT-001 merged · meow-runtime: embeds V8 (deno_core 0.403 / v8 149), isolate + event loop + op layer; `meow run` executes ESM through V8 (P0 exit "trivial ESM" met); never panics on user JS (I-6); refuses first-party .cjs (I-2) · gates: floor green (64 tests), craft green (2 fixed: .cjs refusal, honest argv), footprint green (V8 linked: gzip 18.3MB ≤ 60MB).
2026-06-07 · RT-003 merged · meow-graph: erasable-only TS type-strip (whitespace-blank via oxc_ast_visit — positions byte-stable, no codegen/sourcemap, I-3/§9); ErasablePolicy rejects enum/namespace/param-properties/import=/export= and is now the GraphDb default; erasability gate unconditional for TS · gates: floor green (81 tests), craft green (3 strip bugs fixed), strip-fidelity green.
2026-06-07 · LOAD-001 merged · meow-loader: the single resolver (I-1/I-5) + content-addressed cache read (integrity-checked, I-7) + strip wiring; `meow run` now strips-and-runs TS end-to-end and resolves deps from the cache (P0 exit met; later install surface pivots to strict `node_modules`) · gates: floor green (33 loader+cli tests), craft green (3 fixed: project-root discovery, duplicate-name ambiguity, meow-cache URL strictness).
2026-06-07 · RT-002 merged · meow-runtime: tokio async I/O ops (op_read_file/op_tcp_connect) behind a capability seam — opt-in, NOT exposed to default user JS (I-6/I-8); io_uring deferred (tokio is the committed layer, ADR-9); zero perf claims (I-11) · gates: floor green (16 runtime tests), craft green (1 block fixed: default I/O exposure), footprint green (gzip 18.9MB ≤ 60MB).
2026-06-07 · P0 FOUNDATIONS COMPLETE — 8 wave-1 specs merged (DIST/PKG/CFG/GRAPH/RT-001/RT-003/LOAD-001/RT-002). The thesis holds end-to-end: `meow run app.ts` strips TypeScript and runs it through V8 with cache-backed dependency resolution. Gates green: footprint, craft, strip-fidelity; floor green (~100 tests). Phase → P1 (Minimal Runtime / MVP).
2026-06-07 · RT-004 merged · meow-runtime: strict-web Stateless-Edge globals (CANON §8.1) wired from unmodified upstream deno extensions (deno_web/deno_crypto/deno_fetch) — fetch/Request/Response/Headers/URL/URLPattern/crypto.subtle/TextEncoder/AbortController/Blob/FormData/setTimeout; fetch network capability-gated (host:port), no DOM; ambient .d.ts feature-gated on `web-fetch` · gates: floor green, craft green (4 fixed: tsconfig-merge, feature-gated types, §8.1 subset, port gate; + a stray out-of-scope RT-006 stripped before merge), footprint green (gzip 26.3MB ≤ 60MB).
2026-06-07 · RT-006 merged (concurrent commit 451855c) · meow-runtime: determinism & hermeticity harness — deterministic-by-default virtual clock + seeded ChaCha20 RNG + env-deny; `meow run --allow-clock/-random/-env` grants; host reads confined to src/hermetic/ (I-6). Floor green; NOT orchestrator-craft-gated — see DF-001 (post-hoc gate pending).
2026-06-07 · CFG-002 merged (concurrent commit 2a97cef) · meow-config: legacy root package.json projection from meow.config (ADR-8 as originally written; superseded by DR-001, cleanup in CFG-003) + `meow doctor` staleness/hand-edit check. Floor green; NOT orchestrator-craft-gated — see DF-001.
2026-06-07 · ⚠ DF-001 (process) — RT-006 + CFG-002 reached `main` via a concurrent committer (a dispatched subagent overstepped the no-commit rule; CFG-002 from a source not dispatched by the orchestrator). Tree verified floor-green and state reconciled; orchestrator craft/reality gating still owed. Coordinate the concurrent committer before further dispatch.
2026-06-08 · LOAD-003 merged (99415f8) · meow-loader: full Node module resolution in THE single resolver (I-1/I-5) — exports/imports/conditions/subpath-patterns/self-reference/main+type/extensionless, reading gzip-tar package archives from the content-addressed cache; meow-cache://<hash>/<member> authority URLs; typed ResolveError on every malformed input (no panic) · gates: floor green (loader 23 + pkg 31 tests, resolution_corpus), craft green (craft-7).
2026-06-08 · RT-005 merged (b2c7500, fix 292e290) · meow-runtime: meow:http serve (hyper over tokio, network capability-gated before bind, synchronous bind so Server.addr resolves) + native meow:* registry + the load-bearing I-9 typegen (`meow types --emit/--check`, tsc-delegated, byte-stable committed .d.ts, editor-resolves-with-nothing-installed) + loader meow: scheme arm · gates: floor green (serve/typegen/import tests), craft green, types-fresh green.
2026-06-08 · P1 MINIMAL RUNTIME (MVP) COMPLETE — 5 wave-2 specs merged (RT-004/RT-006/CFG-002/LOAD-003/RT-005). MVP exit MET: `meow run` a TS meow:http web server end-to-end through V8, no emitted JS, deterministic by default. Gates green: floor (~22 suites), craft (craft-7 PASS; 3 non-blocking majors → DF-002), footprint (gzip 25MB ≤ 60MB), types-fresh, resolver-parity, strip-fidelity, determinism. Phase → P2 (Packages & Resolution).
2026-06-08 · craft-7 response (c4a62e3) · honest-boundary prose for the craft-gate majors: named the TZ/locale determinism residual (RT-006) and the condition fixed-priority divergence from Node (LOAD-003); structural follow-ups (unify the fetch/serve capability handle, pin V8 to UTC, order-preserving conditions) recorded as DF-002.
2026-06-08 · RT-006 craft-7 M3 closed (b04cc91) · TZ/locale determinism residual closed: hermetic::pin_deterministic_intl pins TZ=UTC + a fixed default locale before the V8 isolate under the virtual clock, so Date/Intl render identically across host timezones+locales (verified fr_FR+NY == ja_JP+Tokyo); --allow-clock opts into the real env. determinism gate now covers Date/Intl rendering.
2026-06-08 · P2 SPECCED — 5 Packages & Resolution specs drafted + committed (147ea7b): PKG-002 install+integrity, PKG-003 ResolutionGraph/PnP, PKG-004 materialize/vendor projection, OBS-001 why-dep, LSP-001 shared-resolver + shadow-symlink map — to the _TEMPLATE depth bar, by parallel spec-drafters IRC-coordinated on the shared ResolutionGraph + Resolver::from_resolution contract (one resolver, I-5). 16 assumption forks → decisions.json. Next: the P2 build cycle (PKG-002 first).
2026-06-08 · PKG-002 merged (8294b74) · meow install — npm resolution (semver max-satisfying + dist-tags), integrity-verified tarball download (verify npm sha512 → meow sha256 = cache key + lockfile integrity, I-7), deterministic meow.lock.jsonl, RegistrySource seam (real ureq+rustls HTTPS at the binary edge + FixtureRegistry for tests); cmd_run derives root_deps from the config. Reality-checked (real npm): `meow install camelcase-keys@^10` → 6 pkgs, `meow run` prints {"fooBar":true}, reinstall byte-identical. Also installs the rustls aws-lc-rs crypto-provider default. · gates: floor green (23 suites), lockfile-integrity + determinism (reality-check), footprint green.
2026-06-08 · OBS-001 merged (60fee0a) · meow why-dep — pure deterministic lockfile dependency-path tracing (new crate meow-obs): chain(s) from a direct dep to the target, All (cycle-safe, --limit-bounded) + Shortest (BFS) modes, multi-version grouping, --json; reports only version+integrity (I-11, not capabilities/cost), builds no second resolver (I-1). CLI `meow why-dep <name> [--shortest|--json|--limit]` flips the stub; honest not-found exit 1; malformed lockfile no panic. Smoke: why-dep camelcase → camelcase-keys@10.0.2 → camelcase@9.0.0. · gates: floor green (26 suites), determinism + honesty + lockfile-integrity. First OBS subsystem deliverable.
2026-06-08 · PKG-003 merged (7305f7c) · ResolutionGraph — the shared in-memory PnP model (new meow_pkg::pnp). `assemble` (cycle-safe BFS closure over the lockfile; typed RootNotLocked/DanglingEdge naming the referrer), `from_project` (read lock + shared resolve_roots + assemble; missing lock = empty graph), `packages()` (root-reachable, sorted, borrowed ResolvedPackage, multi-version preserved, orphans excluded), `verify_cached` (presence only — integrity stays lazy at Cache::read, I-7). `Resolver::from_resolution` seam: runtime + LSP build the one resolver from the SAME Arc<ResolutionGraph> (resolver-parity I-5; one resolver I-1). `meow run` now resolves via assemble + verify_cached + from_resolution. · gates: floor green.
2026-06-08 · PKG-004 merged (2c3f81b) · meow install materialize/vendor — the disk-projection layer over the SAME ResolutionGraph. `Materializer` + `plan`/`tree_hash` (pure, path-sorted, iteration-order-independent, I-6) + atomic temp-then-rename + `Cache::read` integrity (tamper aborts with nothing written, I-7) + `.materialized` sidecar idempotence; isolated pnpm-style no-hoist layout so a Node walker resolves the exact version meow's graph does (resolver-parity, I-5). materialize = node_modules + symlink edges, vendor = copy-only self-contained. Hardened streaming gzip-tar unpack rejects unsafe members. · gates: floor green.
2026-06-08 · LSP-001 merged (db086b2) · meow-lsp — the resolver-parity keystone + last P2 spec. EditorResolver holds THE meow_loader::Resolver (built via from_resolution from the SHARED ResolutionGraph) and delegates to `locate` — no second resolver/parser (I-1, I-5); maps editor_path into the unpacked store. ShadowDeps generates `.meow/deps/` directory symlinks into the global UnpackedStore (CANON §20.1) so TS-server traverses the cache — atomic (deps.tmp->rename), integrity-gated (I-7), idempotent/drift-free, multi-version-honest (collisions). Added meow_pkg::UnpackedStore + MaterializeError::CacheBlob. · gates: floor green.

2026-06-08 · === P2 (Packages & Resolution) COMPLETE === · all 5 specs merged + gated (PKG-002 install, OBS-001 why-dep, PKG-003 ResolutionGraph, PKG-004 materialize/vendor, LSP-001 shared-resolver + shadow map). Exit criteria met for cache-backed install/run and resolver parity; current pivot makes strict symlinked `node_modules` the default install projection. Gates resolver-parity + lockfile-integrity green; footprint 27.5MB. Next phase pointer superseded by DR-003: P2.5 Drop-In Core before P3.
